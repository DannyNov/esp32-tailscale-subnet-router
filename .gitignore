# Secrets - DO NOT COMMIT
secrets/
secrets.h
secrets.yaml
*.key
*.pem
.cf-token
.cf-account
.dev.vars
.ts-api-token
ts-devices.json

# Build artifacts
build/
build_eth/
.pio/
.pio*
managed_components/
dependencies.lock
sdkconfig
sdkconfig.old
sdkconfig.esp32*
*.bin
*.elf
*.map

# IDE
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc

# Keep these
!sdkconfig.defaults
!sdkconfig.defaults.*

# Local-only working artifacts
backup/
design-prototype/.draft/
build.log
.env
.env.local
# Playwright smoke scripts + outputs — embed device creds + decrypted secrets
tools/_pw_*
# Mockup / brand / screenshot scratch — embeds device creds + raw (unscrubbed)
# captures and real config backups; never commit. Promoted assets live under
# assets/ and docs/images/ (scrubbed) instead.
tools/_mockups/

# Dev scratch & agent state — NEVER commit. These contain the device admin
# password, the operator email, real names, the tailnet name, real SSIDs,
# dev-network IPs and exit-node IPs. None of it is part of the firmware.
/.omc/
/.claude/
*HANDOFF*.md
*HANDOFF*.txt
/SDLOG_PLAN.md
/backup_pre_repartition/
/telemetry/
/build_*.log
/flash_*.log
/sdlog_*.txt
/sdlog_*.log

# Generated by main/CMakeLists.txt from main/index.html
main/index_html_data.c
/index_html_data.c
.tools/
/tests/host/build/
/tests/host/*.exe
/edit_*.py
